language: python

python:
  - "3.5"
  - "3.6"

install:
  - python setup.py install
  - if [[ $TRAVIS_PYTHON_VERSION == '3.6' ]]; then pip install coveralls --use-mirrors && export HAS_COVERALLS=1; fi

script: python setup.py test

after_success:
  - if [[ $HAS_COVERALLS ]]; then coveralls; fi
